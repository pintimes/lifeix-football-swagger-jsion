{
    "summary": "用户注册",
    "tags": 
    [
        "user"
    ],

    "description": "用户注册",
    "produces": 
    [
        "application/json"
    ],

    "parameters": 
    [
        {
            "name": "deviceId",
            "in": "query",
            "type": "string",
            "required": false,
            "description": "设备号"
        },
        {
            "name": "deviceToken",
            "in": "query",
            "type": "string",
            "required": false,
            "description": "设备标志"
        },
        {
            "name": "role",
            "in": "query",
            "type": "string",
            "required": true,
            "description": "角色"
        },
        {
            "schema": 
            {
                "$ref": "#/definitions/User"
            },
            "description": "用户",
            "name": "user",
            "required": true,
            "in": "body"
        }
    ],

    "consumes": 
    [
        "application/json"
    ],

    "responses": 
    {
        "200": 
        {
            "schema": 
            {
                "$ref": "#/definitions/User"
            },

            "description": "User响应对象"
        }
    },

    "operationId": "addUser"
}